import os
import sys
import math


def clear_screen():
    """Очистка консоли"""
    os.system('cls' if os.name == 'nt' else 'clear')


def print_header(title):
    """Красивая рамка для заголовков"""
    print("\n" + "=" * 55)
    print(f"║ {title.center(51)} ║")
    print("=" * 55)


# =========================================================
# ЗАДАНИЕ №2
# =========================================================

def task_2():
    clear_screen()
    print_header("ЗАДАНИЕ №2: РАСЧЕТ ПРЯМОУГОЛЬНИКА")

    try:
        a = float(input("  ► Введите длину прямоугольника: "))
        b = float(input("  ► Введите ширину прямоугольника: "))

        if a <= 0 or b <= 0:
            print("\n  [!] Ошибка: стороны должны быть больше нуля!")
            return

        S = a * b
        P = 2 * (a + b)

        print("\n" + "-" * 55)
        print("  ✔ Результаты вычислений:")
        print(f"    • Площадь (S) : {S:,.2f}")
        print(f"    • Периметр (P): {P:,.2f}")
        print("-" * 55)

    except ValueError:
        print("\n  [!] Ошибка: пожалуйста, вводите только числа.")


# =========================================================
# ЗАДАНИЕ №10
# =========================================================

def task_10():
    clear_screen()
    print_header("ЗАДАНИЕ №10: ПОРАЗРЯДНЫЙ РАЗБОР ЧИСЛА")

    try:
        num = int(input("  ► Введите четырехзначное целое число: "))

        if not (1000 <= abs(num) <= 9999):
            print("\n  [!] Ошибка: число должно содержать ровно 4 цифры!")
            return

        num = abs(num)

        thousands = num // 1000
        hundreds = (num % 1000) // 100
        tens = (num % 100) // 10
        units = num % 10

        print("\n" + "-" * 55)
        print("  ✔ Выделенные разряды:")
        print(f"    • Тысячи  : {thousands}")
        print(f"    • Сотни   : {hundreds}")
        print(f"    • Десятки : {tens}")
        print(f"    • Единицы : {units}")
        print("-" * 55)

    except ValueError:
        print("\n  [!] Ошибка: введено не целое число!")


# =========================================================
# ЗАДАНИЕ №19
# =========================================================

def task_19():
    clear_screen()
    print_header("ЗАДАНИЕ №19: ПРЯМОУГОЛЬНЫЙ ТРЕУГОЛЬНИК")

    try:
        a = float(input("  ► Введите первый катет: "))
        b = float(input("  ► Введите второй катет: "))

        if a <= 0 or b <= 0:
            print("\n  [!] Ошибка: катеты должны быть больше нуля!")
            return

        # Гипотенуза
        c = math.sqrt(a ** 2 + b ** 2)

        # Площадь
        S = (a * b) / 2

        # Периметр
        P = a + b + c

        # Острые углы
        angle_a = math.degrees(math.atan(a / b))
        angle_b = math.degrees(math.atan(b / a))

        print("\n" + "-" * 55)
        print("  ✔ Результаты вычислений:")
        print(f"    • Гипотенуза : {c:,.2f}")
        print(f"    • Площадь    : {S:,.2f}")
        print(f"    • Периметр   : {P:,.2f}")
        print(f"    • Угол 1     : {angle_a:,.2f}°")
        print(f"    • Угол 2     : {angle_b:,.2f}°")
        print("-" * 55)

    except ValueError:
        print("\n  [!] Ошибка: пожалуйста, вводите только числа.")


# =========================================================
# ГЛАВНОЕ МЕНЮ
# =========================================================

def main_menu():

    while True:
        clear_screen()

        print("=" * 55)
        print("║" + " ГЛАВНОЕ МЕНЮ ЛАБОРАТОРНОЙ РАБОТЫ ".center(53) + "║")
        print("=" * 55)

        print("  [1] Задание №2  (Площадь и периметр прямоугольника)")
        print("  [2] Задание №10 (Разбор четырехзначного числа)")
        print("  [3] Задание №19 (Прямоугольный треугольник)")
        print("  [0] Выход из программы")

        print("=" * 55)

        choice = input("  ► Выберите номер пункта меню: ").strip()

        if choice == '1':
            task_2()

        elif choice == '2':
            task_10()

        elif choice == '3':
            task_19()

        elif choice == '0':
            clear_screen()
            print("\n  Программа успешно завершена. Всего доброго!\n")
            sys.exit()

        else:
            print("\n  [!] Неверный пункт меню.")

        input("\n  Нажмите Enter для возврата в меню...")


# =========================================================
# ЗАПУСК ПРОГРАММЫ
# =========================================================

if __name__ == "__main__":
    main_menu()
